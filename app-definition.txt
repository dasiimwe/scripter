





- section to manage scripts ()
- the app should have a section for script generation using jinja2. Where the scripts are stored as jinja2 tempates and the user can fill a form to genrete the script.
- the app should have a feature to extract fields from the templete and add those fields to the form for that script.


- the app should have the abiliy to push scripts to network devices via ssh, telnet or api

- the app shoudl have device connection definition section where you define the type of connection (ssh, telnet or api)

- define device configuration tests which are commands that are sent to devices and user can define logical operations 
and actions to perform based on the output
    for examples 
    - run script 1 on device 1 if output contains 'failed' test = failed.
    - run script 2 on device 3 if output contains 'sdwan' run action name 'named'


- have a section for managing actions so additional actions can be easily added. example of an action is run script, return output, stop, email, api call, add to calendat, remove from calendar, send to teams chat, start webex meeting, open vendor ticket

- have a section for managing sites where sites have a site name, address , site code and unlimited number of custom fields

- have a section for managing network interfaces. each interface should have ip address, subnet, mac address, vrf, description, secondary_ip and unlimited number of custom fields.

- have a section for managing circuits. each circuit should have circuit provider, circuit name, cicuit id, bandwidth and unlimited number of custom fields.

- have a section for managing devices where each devices belongs to a site, has a device name, manufacturer, credential_id, snmp_creditial_id, mgmt ip and unlimited number of custom fields.

- have a section for managing credentials including ssh credentials, api credentials, ssh keys, snmp crednetials

- have a section for network IPAM management where the user can view assigned IPs and available IPs.



each section should have add, edit, delete functionality and table view that has pagination, sorting and search
app shoud be developed with a dev, staging and prod environments.
app should be build for scalibity for managing large number of devices.
app architecture should abstrast as much a posible to allow for addition of additional features and functionality
